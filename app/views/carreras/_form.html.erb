<%= form_for(@carrera) do |f| %>
  <% if @carrera.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@carrera.errors.count, "error") %> prohibited this carrera from being saved:</h2>

      <ul>
      <% @carrera.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_area :nombre %>
  </div>
  <div class="field">
    <%= f.label :semestres %><br />
    <%= f.number_field :semestres %>
  </div>
  <div class="field">
    <%= f.label :facultad %><br />
    <%= f.number_field :facultad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
