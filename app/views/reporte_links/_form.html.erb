<%= form_for(@reporte_link) do |f| %>
  <% if @reporte_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reporte_link.errors.count, "error") %> prohibited this reporte_link from being saved:</h2>

      <ul>
      <% @reporte_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario %><br />
    <%= f.number_field :usuario %>
  </div>
  <div class="field">
    <%= f.label :link %><br />
    <%= f.number_field :link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
