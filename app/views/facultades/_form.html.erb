<%= form_for(@facultade) do |f| %>
  <% if @facultade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facultade.errors.count, "error") %> prohibited this facultade from being saved:</h2>

      <ul>
      <% @facultade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_area :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
